document.addEventListener('DOMContentLoaded', async () => {
    // send refresh request for paymentRequest
    /*
    const response = await fetch(`/api/paymentRequest/check`, {
        method: 'PUT',
    });
    console.log(response);
    */

})